## MDM Vendor Details

- **Vendor name:**
- **MDM server URL pattern(s):**
- **Vendor product page / docs URL:**

## How was the URL pattern obtained?

<!-- e.g. "Ran `profiles status -type enrollment` on a macOS device managed by [vendor]" -->

## Checklist

- [ ] Entry added to `mdm_urls.json` with a kebab-case key and `vendor`, `url_patterns`, `info_url`
- [ ] Keys are sorted alphabetically
- [ ] JSON is valid (e.g. `python3 -m json.tool mdm_urls.json`)
